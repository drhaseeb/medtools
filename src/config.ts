export const config = {
  name: "Doctors' Medical ORG",
  shortName: "Doctors' Medical ORG",
  tagline: "Evidence-based clinical calculators, built for the bedside.",
  clinicUrl: "https://clinic.doctorsmedical.org.pk",
  email: "contact@doctorsmedical.org.pk",
  adClient: "ca-pub-6579850399965802",
  // TODO(user): create these as "Display ad" units in AdSense (Ads > By ad
  // unit > Display ads) and paste each unit's data-ad-slot value here — ads
  // won't render until real slot IDs are in place.
  adSlots: {
    afterTool: "",
    sidebar: "",
  },
} as const;
